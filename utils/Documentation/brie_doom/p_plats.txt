note
	description: "[
		p_plats.c
		
		Plats (i.e. elevator platforms) code, raising/lowering
	]"

class 
	P_PLATS

create 
	default_create

feature 

	ev_doplat (line: LINE_T; type: INTEGER_32; amount: INTEGER_32): BOOLEAN
			-- Do Platforms
			-- `amount` is only used for SOME platforms.
		do
			{I_MAIN}.i_error ("EV_DoPlat not implemented")
		ensure
			instance_free: class
		end
	
end -- class P_PLATS
