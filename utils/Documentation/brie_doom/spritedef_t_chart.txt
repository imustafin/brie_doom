class
	SPRITEDEF_T

General
	cluster: brie_doom
	description: 
		"spritedef_t from r_defs.h
		
		A sprite definition:
		a number of animation frames"

Ancestors
	ANY

Queries
	numframes: INTEGER_32
	spriteframes: detachable ARRAY [SPRITEFRAME_T]

Commands
	set_numframes (a_numframes: [like numframes] INTEGER_32)
	set_spriteframes (a_spriteframes: [like spriteframes] detachable ARRAY [SPRITEFRAME_T])

