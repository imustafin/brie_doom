note
	description: "General interface for a bound byte sequence"

deferred class 
	BYTE_SEQUENCE

feature 

	get alias "[]" (index: INTEGER_32): NATURAL_8
		require
				valid_index (index)
		deferred
		end

	valid_index (i: INTEGER_32): BOOLEAN
		deferred
		end
	
end -- class BYTE_SEQUENCE
